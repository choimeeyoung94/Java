package chap01_class_instance;

public class Book {
  
  // 클래스는 값(필드)와 기능(메서드)을 가지는 코드 블록이다
  
  // 클래스가 가지고 있는 값(필드), (메서드)를 사용하려면 일반적으로 
  // 해당 클래스를 타입으로 가지는 객체(인스턴스)를 생성한다
  
  // 생성한 객체(인스턴스)를 이용해서 값(필드)을 사용하거나 기능(메소드)를 호출한다
}
